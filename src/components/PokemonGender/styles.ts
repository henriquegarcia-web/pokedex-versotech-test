import styled from 'styled-components'
import Colors from '@/utils/styles/colors'

export const PokemonGender = styled.span`
  display: flex;
  column-gap: 5px;

  svg {
    font-size: 15px;

    color: ${Colors.textPrimary};
  }
`
